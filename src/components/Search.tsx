import { Input } from "antd";
import "assets/styles/components/Search.css";

const Search = () => (
  <Input className="search" placeholder="Search any fields of the columns" />
);

export default Search;
